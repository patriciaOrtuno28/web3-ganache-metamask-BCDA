import AlumnosList from "./AlumnosList";

const AlumnosPage = () => {
    return (
        <section className="AppAlumnos">
            <h2>Alumnos</h2>

            <AlumnosList/>
        </section>
    );
};

export default AlumnosPage;
